<UserControl x:Class="SelfServiceReportPrinter.Views.Print"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SelfServiceReportPrinter.Views"
      mc:Ignorable="d" 
      xmlns:helper="clr-namespace:SelfServiceReportPrinter.Helper"
      xmlns:vm="clr-namespace:SelfServiceReportPrinter.ViewModels"
      d:DataContext="{d:DesignInstance Type=vm:PrintViewModel}"
      d:DesignHeight="450" d:DesignWidth="800"
      >
    
    <UserControl.Resources>
        <helper:StatusToVisibilityConverter x:Key="StatusToVisibility" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="80" />
        </Grid.RowDefinitions>
        <DataGrid x:Name="one"  SelectionUnit="Cell" Grid.Row="0"  IsReadOnly="True"  Margin="70" AutoGenerateColumns="False" Background="Transparent" HeadersVisibility="None" CanUserAddRows="False"  CanUserSortColumns="False" BorderThickness="0" ItemsSource="{Binding Datalist, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <DataGrid.Resources>
                <ResourceDictionary>
                    <Style x:Key="{x:Type DataGridCell}" TargetType="{x:Type DataGridCell}">
                        <Setter Property="Background" Value="{x:Null}" />
                        <Setter Property="BorderBrush" Value="{x:Null}" />
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{x:Null}" />
                                <Setter Property="BorderBrush" Value="{x:Null}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                    <Style TargetType="{x:Type DataGridRow}">
                        <Setter Property="Background" Value="{x:Null}" />
                        <Setter Property="BorderBrush" Value="{x:Null}" />
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{x:Null}" />
                                <Setter Property="BorderBrush" Value="{x:Null}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                    <Style x:Key="CenteredTextBlockStyle" TargetType="{x:Type TextBlock}">
                        <Setter Property="HorizontalAlignment" Value="Center"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                    </Style>
                </ResourceDictionary>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="姓名"  Binding="{Binding Name}" Width="*"  />
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="120" />
                <DataGridTemplateColumn Header="操作" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <Button Background="#62bdca" Foreground="#ffffff" x:Name="print" Content="打印" Width="60" Margin="3" Command="{Binding PrintCommand}" Height="25" Visibility="{Binding Status,Converter={StaticResource StatusToVisibility}}"></Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="1" HorizontalAlignment="Center"  Orientation="Horizontal">
            <TextBlock Text="倒计时："  VerticalAlignment="Center"></TextBlock>
            <TextBlock Text="{Binding CountDownText}" VerticalAlignment="Center" FontSize="24" />
            <Button Content="返回" Command="{Binding GoBackCommand}"></Button>
        </StackPanel>
    </Grid>
</UserControl>
