<Window x:Class="SelfServiceReportPrinter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:selfservicereportprinter="clr-namespace:SelfServiceReportPrinter"      
        xmlns:vm="clr-namespace:SelfServiceReportPrinter.ViewModels" 
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen" 
        xmlns:local="clr-namespace:SelfServiceReportPrinter.Views"       
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        AllowsTransparency="True"
        WindowStyle="None"
        Title="MainWindow" 
        Height="1080" 
        Width="1920">
    <Grid >
        <Grid.Background>
            <ImageBrush ImageSource="pack://application:,,,/Images/background.png"   Stretch="UniformToFill"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0"  Style="{StaticResource TitleBarStyle}" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"  VerticalAlignment="Center" Text="神马医院" FontSize="18" Foreground="White"/>
                <TextBlock Grid.Column="1" HorizontalAlignment="Center" Text="自助报告打印系统"  FontSize="28" Foreground="White"/>
                <!--<Button Grid.Column="2" Content="打印" Background="#62bdca" Foreground="White"   FontSize="14" Width="60" Height="30" Command="{Binding PrintCommand}"/>-->
            </Grid>
        </Border>
        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type vm:KeyPressViewModelCommunityToolkit}">
                    <local:Home />
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:PrintViewModel}">
                    <local:Print />
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</Window>
